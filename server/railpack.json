{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["npm install --omit=dev"]
    }
  },
  "deploy": {
    "startCommand": "node src/server.js"
  }
}
