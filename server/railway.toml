[build]
builder = "NIXPACKS"
buildCommand = "npm ci --omit=dev || npm install --omit=dev && test -f src/db/init.js"

[deploy]
startCommand = "node src/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
